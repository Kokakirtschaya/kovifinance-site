/** Почти на всю ширину окна: отступы растут с экраном, потолка в пикселях нет.
    Иначе на ультраwide / при отдалении контент схлопывается в узкий островок. */
export const SHELL =
  "mx-auto w-full px-5 sm:px-6 lg:px-[max(2rem,4vw)] 2xl:px-[max(2.5rem,5vw)]";
